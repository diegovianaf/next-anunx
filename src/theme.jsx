import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: '#000',
    },
    background: {
      default: '#f2f4f5',
      white: '#fff',
    },
  },
})

export default theme
